import 'package:flutter/material.dart';

class SelectMatiere extends StatelessWidget {
  const SelectMatiere({ Key? key, this.name, this.level }) : super(key: key);
  final name;
  final level;
  @override
  Widget build(BuildContext context) {
    return Scaffold(
    
      
    );
  }
}